// Deprecated compatibility shim.
// The project now uses OpenRouter through the official OpenAI SDK instead of
// @anthropic-ai/claude-agent-sdk. Prefer importing OpenRouterProvider directly.
export { OpenRouterProvider as ClaudeAgentSDKProvider } from "./openRouterProvider";
